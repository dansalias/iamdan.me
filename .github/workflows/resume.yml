name: compile-resume

on:
  push:
    branches:
      - trunk
    paths:
      - '/resume'

jobs:
  compile:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: lvignoli/typst-action@main
        with:
          source_file: resume/resume.typ
